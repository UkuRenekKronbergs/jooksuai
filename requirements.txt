streamlit>=1.32
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
plotly>=5.18
python-dotenv>=1.0
anthropic>=0.39
openai>=1.40
stravalib>=1.7
requests>=2.31
pydantic>=2.6
tabulate>=0.9

# Install the local `vorm` package itself (src-layout, declared in pyproject.toml).
# Streamlit Cloud only runs `pip install -r requirements.txt`, so without this
# line `from vorm.config import ...` fails with ModuleNotFoundError.
.
